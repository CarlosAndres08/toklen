name: toklen_app
description: "TOKLEN professional services frontend."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.11.5

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^3.3.1
  http: ^1.6.0
  http_parser: ^4.1.2
  image_picker: ^1.2.2
  cached_network_image: ^3.4.1
  shared_preferences: ^2.5.5
  go_router: ^14.0.0 # O la versión más reciente
  flutter_svg: ^2.0.10
  intl: ^0.19.0
  web_socket_channel: ^3.0.1
  geolocator: ^13.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/  

  